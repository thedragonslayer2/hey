repeat wait() until game:IsLoaded()
getgenv().buttonclick = function(button)
	spawn(function()
		local events = { "MouseButton1Click", "MouseButton1Down", "Activated" }
		for i, v in pairs(events) do
			pcall(function()
				for i, v in pairs(getconnections(button[v])) do pcall(function() v:Fire() end) end
			end)
		end
	end)
end
getgenv().players = game:GetService"Players"
getgenv().client = game:GetService"Players".LocalPlayer
local Queue = queue_on_teleport or syn and syn.queue_on_teleport
if Queue and not AlreadyAutoExecute then
	Queue'repeat wait() until game:IsLoaded()\nloadstring(game:HttpGet"https://pastebinp.com/raw/eccv9pgu")()'
	getgenv().AlreadyAutoExecute = true
end
