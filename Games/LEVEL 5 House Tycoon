if not firetouchinterest then
    IrisNotificationMrJack(2, "Key System", "Your Exploit Is Not Supported!\n\nDownload Krnl,\nThe Most Powerful Free Exploit!", 7)
    return
end
local library = loadstring(game:HttpGet"https://raw.githubusercontent.com/thedragonslayer2/lib/main/UwU%20Ui")()
local players = game:GetService"Players"
local client = players.LocalPlayer
local runService = game:GetService"RunService"
local autofarm = library:CreateWindow"House Tycoon"
autofarm:AddToggle{ text = "Build", flag = "autobuild" }
autofarm:AddToggle{ text = "Collect Cash", flag = "cash" }
autofarm:AddToggle{ text = "Anti-Afk", flag = "afk" }
autofarm:AddLabel{ text = "RightAlt To Hide/Show Gui" }

local function gettycoon()
    for i, v in next, workspace["Humza's Tycoon Kit"].Tycoons:GetChildren() do
        if tostring(v.Owner.Value) == client.Name then return v end
        wait()
    end
    return nil
end

runService.Stepped:connect(function()
    if library.flags.afk then game.Players.LocalPlayer.Character.Humanoid:ChangeState(11) end
end)
spawn(function()
    while wait(0.5) do
        pcall(function()
            local tycoon = gettycoon()
            if library.flags.cash and tycoon then
                firetouchinterest(client.Character.HumanoidRootPart, tycoon.Essentials.Giver, 0)
                firetouchinterest(client.Character.HumanoidRootPart, tycoon.Essentials.Giver, 1)
            end
        end)
    end
end)
spawn(function()
    local oncwa
    while wait() do
        pcall(function()
            local tycoon = gettycoon()
            if library.flags.afk and tycoon then
                client.Character.HumanoidRootPart.CFrame = tycoon.Essentials.Giver.CFrame * CFrame.new(0, 8, 0) * CFrame.Angles(0, 0, 3.1)
                game:GetService"VirtualInputManager":SendKeyEvent(true, "W", false, game)
                game:GetService"VirtualInputManager":SendKeyEvent(true, "Right", false, game)
                oncwa = true
                wait(2)
            elseif oncwa then
                wait(0.3)
                client.Character.HumanoidRootPart.CFrame = tycoon.Essentials.Spawn.CFrame * CFrame.new(0, 4, 0)
                oncwa = false
            end
        end)
    end
end)
spawn(function()
    while wait() do
        pcall(function()
            local tycoon = gettycoon()
            if library.flags.autobuild and tycoon then
                for i, v in next, tycoon.Buttons:GetChildren() do
                    if v:FindFirstChild"Head" and library.flags.autobuild then
                        firetouchinterest(client.Character.HumanoidRootPart, v.Head, 0)
                        firetouchinterest(client.Character.HumanoidRootPart, v.Head, 1)
                        pcall(function()
                            game:GetService"CoreGui".PurchasePromptApp.ProductPurchase.Animator:Destroy()
                        end)
                    end
                    if string.find(tostring(i), "50$") or string.find(tostring(i), "00$") then wait() end
                end
            end
        end)
    end
end)
library:Init()
if not passlolbruh then
    if not IrisNotificationMrJack then
        loadstring(game:HttpGet"https://raw.githubusercontent.com/thedragonslayer2/hey/main/Misc./iris%20notification%20function")()
    elseif IrisNotificationUserMrJack then IrisNotificationUserMrJack.ClearAllNotifications() end
    IrisNotificationMrJack(2, "Test", "Something went wrong,\nPlease Execute again!", 9e9)
    while wait() do end
end
return true
